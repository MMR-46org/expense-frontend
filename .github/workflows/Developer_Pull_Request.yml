name: Developer_Pull_Requests

on:
  pull_request:
    branches:
      - 'main'

jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: unit tests
        run: echo unit tests

      - name: integration tests
        run: echo integration tests

      - name: SAST scans
        run: echo SAST Scans

      - name: Detect Secrets in source code
        run: echo detect secrets in source code

      - name: code quality
        run: echo code quality
